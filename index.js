const userName = 'arif';
const userAge = 32;
const userInfo = `${userName} is a good person and he is ${userAge} years old`;
// console.log(userInfo);
const numberIntoString = userAge.toString();
console.log(numberIntoString);
export {};
