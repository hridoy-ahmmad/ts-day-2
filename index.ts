const userName: string = 'arif';
const userAge: number = 32

const userInfo: string | number = `${userName} is a good person and he is ${userAge} years old`
// console.log(userInfo);
const numberIntoString: string = userAge.toString()
console.log(numberIntoString);


export { }